import React from 'react';
import {View} from 'react-native';

export const HTMLContentModel = props => <View {...props} />;

export const HTMLElementModel = props => <View {...props} />;

const RNRenderHTMLMock = props => <View {...props} />;

export default RNRenderHTMLMock;
