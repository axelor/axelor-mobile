import React from 'react';
import {View} from 'react-native';

export const actions = jest.fn();

export const RichEditor = props => <View {...props} />;

export const RichToolbar = props => <View {...props} />;
