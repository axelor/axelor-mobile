import React from 'react';
import {storiesOf} from '@storybook/react-native';
import HeaderIndicator from './HeaderIndicator';

storiesOf('ui/molecules/HeaderIndicator', module).add('Default', () => (
  <HeaderIndicator />
));
