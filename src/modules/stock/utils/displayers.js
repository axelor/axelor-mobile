export const displayItemName = item => item.name;
export const displayStockMoveSeq = item => item.stockMoveSeq;
export const displayItemTrackingNumber = item => item.trackingNumberSeq;
export const displayInventorySeq = item => item.inventorySeq;
export const displayPartner = item => item.fullName;
